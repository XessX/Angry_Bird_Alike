/**
 * @file main.cpp
 * @brief Entry point for the simulation game.
 *
 * Initializes and runs the game.
 */

/**
 * Main function.
 * @return int Exit status code. Returns 0 on successful execution.
 */


#include "../include/Game.h"

int main() {
    Game game;
    game.run();
    return 0;
}
/**
 * Main function, the entry point of the application.
 * @return int The exit code of the application, where 0 indicates successful termination.
 */